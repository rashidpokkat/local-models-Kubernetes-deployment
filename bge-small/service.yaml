apiVersion: v1
kind: Service
metadata:
  name: bge-small
  namespace: bge-small
  labels:
    app.kubernetes.io/name: bge-small
    app.kubernetes.io/component: embeddings
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bge-small
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
