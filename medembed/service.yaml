apiVersion: v1
kind: Service
metadata:
  name: medembed
  namespace: medembed
  labels:
    app.kubernetes.io/name: medembed
    app.kubernetes.io/component: embeddings
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: medembed
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
